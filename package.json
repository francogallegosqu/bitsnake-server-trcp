{
  "name": "trpc-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predev": "npm run prisma:migrate",
    "dev": "concurrently -k -r  \"npm:prisma:studio\" \"npm:server\"",
    "prisma:migrate": "npx prisma migrate dev --name init",
    "prisma:generate": "npx prisma generate",
    "prisma:studio": "npx prisma studio",
    "local": "ts-node-dev -r tsconfig-paths/register --poll --respawn --transpile-only --no-deps --exit-child src/server.ts",
    "server": "ts-node-dev -r tsconfig-paths/register --poll --respawn --transpile-only --exit-child src/server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cookie": "^7.3.1",
    "@prisma/client": "^4.1.1",
    "@trpc/client": "^9.26.2",
    "@trpc/server": "^9.26.2",
    "bcrypt": "^5.0.1",
    "cookie": "^0.5.0",
    "fastify": "^4.3.0",
    "jsonwebtoken": "^8.5.1",
    "superjson": "^1.9.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4",
    "zod": "^3.17.10"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/jsonwebtoken": "^8.5.8",
    "@types/node": "^18.6.3",
    "concurrently": "^7.2.2",
    "prisma": "^4.1.1",
    "tsconfig-paths": "^4.1.0"
  }
}
